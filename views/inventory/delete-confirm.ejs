<h1 class="delete-title">Delete <%= inv_make %> <%= inv_model %></h1>

<p class="delete-warning">
    This action is permanent. Once deleted, this vehicle cannot be recovered.
</p>

<form action="/inv/delete" method="post" class="delete-form">
    
    <div class="delete-field">
        <label>Make</label>
        <input type="text" value="<%= inv_make %>" readonly>
    </div>

    <div class="delete-field">
        <label>Model</label>
        <input type="text" value="<%= inv_model %>" readonly>
    </div>

    <div class="delete-field">
        <label>Year</label>
        <input type="text" value="<%= inv_year %>" readonly>
    </div>

    <div class="delete-field">
        <label>Description</label>
        <textarea readonly><%= inv_description %></textarea>
    </div>

    <input type="hidden" name="inv_id" value="<%= inv_id %>">

    <button type="submit" class="delete-btn">Delete Vehicle</button>
</form>
<p><a href="/inv/management">Cancel</a></p>